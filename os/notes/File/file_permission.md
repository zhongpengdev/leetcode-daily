# 文件权限

访问控制的本质就是解决主体（进程绑定的用户凭证）对客体（文件系统中的 inode 及关联数据块）进行操作合法性的判定机制的权限匹配问题。

常见访问权限：
读：从文件中读数据
写：向文件中写数据
执行：将件装入内存并执行
添加：将新信息添加到件结尾部分
删除：删除件，释放空间
列表清单：列出文件名和文件属性

静态的用户和用户组保存在 `/etc/passwd` `/etc/group`中，只有当用户运行程序，成为进程时才会触发权限检查。内核在该进程的PCB中记录了进程继承的身份凭证：

+ UID：当前进程属于哪个用户
+ GID：当前进程属于哪个组

被访问资源中，每个文件/目录都对应一个唯一的inode（索引节点），包含了：
+ i_uid：文件属于哪个用户
+ i_gid：文件属于哪个组
+ i_mode：各类用户对他的操作
  



## UGO权限

User
Group
Others



Q：加密保护和访问控制在保障安全时有各自的计算开销。在高性能高并发的分布式文件系统（如 HDFS、Ceph）中，如何权衡这两者的使用？是否可以相互替代？

Q：在 11408 操作系统真题考点中，目录也是一种特殊的文件。假设用户 userA 对目录 /dir 具有 写权限（w），但对该目录下的文件 /dir/test.txt 没有任何权限（即 ---）。请反思：userA 此时能否通过终端强制执行 rm /dir/test.txt 成功删除该文件？请从操作系统目录项（Directory Entry）与文件数据的解耦原理来解释原因。