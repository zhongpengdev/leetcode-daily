# 读策略
解决的是：数据怎么从数据库进缓存

1. Cache-Aside 缓存旁路
   
缓存不主动做任何事，它只是被动地躺在旁边。所有读写逻辑，都是由应用程序（也就是你写的代码）主动去操控它。

场景一：有人来读操作：缓存命中，直接返回，缓存未命中，先查数据库，写入缓存，返回。

场景二：有人来写操作：程序直接修改数据库，不更新缓存，直接删掉缓存。（如果同一个key反复写了很多次，无用开销很大。）

2. Read-Through